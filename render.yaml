services:
  - type: web
    name: carrec-ai
    env: docker
    region: oregon
    plan: free
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PORT
        value: 7860
      - key: OPENAI_API_KEY
        sync: false
