name: Keep Alive

on:
  schedule:
    - cron: "0 */6 * * *"

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://carrec-ai.onrender.com/health || true
